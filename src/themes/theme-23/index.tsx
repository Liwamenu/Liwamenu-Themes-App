/**
 * Theme 23 — Green Border (clean grid-card menu)
 *
 * Bright leaf-green primary on a soft warm-gray canvas. Inter throughout
 * (no display scripts), 2-column product cards that lead with a circular
 * green-tinted photo on top, bold name + Inter description + green price
 * underneath. Subcategory navigation uses white pill buttons with a
 * green underline on the active one — straight from the reference design.
 */

import "./theme.css";
export { MenuPage as default } from "./MenuPage";
