/**
 * Theme 25 — Orange Fresh (Premium "Orange White" reference)
 *
 * Vibrant orange canvas with floating white cards (rounded-[32px]) and
 * a dense shadow stack. Filter pills rounded-full (active = solid
 * orange + white, inactive = light-orange + orange text). Product
 * cards: white tile with heart top-left, square photo, center-aligned
 * bold name + price + 5-star rating row. ProductDetailModal: centered
 * hero image with circular qty badge, carousel dots, big centered
 * title, square +/- qty buttons flanking the total, About block, two
 * orange stat chips, full-width ADD TO CART button. Poppins typeface.
 */

import "./theme.css";
export { MenuPage as default } from "./MenuPage";
